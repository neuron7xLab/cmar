<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# Pipeline

```
intent -> scan -> normalize -> quantize -> voids -> plan -> repair/autofill -> protocol -> integrate -> ledger -> verdict
```
Each arrow is a real function; `cmar integrate` chains their digests into one root hash.
